import { AdminGamerDirectory } from "@/components/admin-gamer-directory";

export const metadata = { title: "Manage Gamers" };

export default function AdminGamersPage() {
  return <AdminGamerDirectory />;
}
